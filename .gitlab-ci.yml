stages:
  - build

docker_build:
  stage: build
  script:
    - docker build -v $(pwd)/:/website -t private.parrot.run/website:latest .
    - docker login -u $DOCKER_REGISTRY_USER -p $DOCKER_REGISTRY_PASSWORD
    - docker push private.parrot.run/website:latest