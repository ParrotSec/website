"use strict";
exports.__esModule = true;
var path_1 = require("path");
var replaceGlobs = function (path) {
    return path.replace(/(\/\*\*)*\/\*$/, "");
};
exports["default"] = function (tsconfigFile, dirname) {
    if (dirname === void 0) { dirname = "."; }
    var _a = tsconfigFile.compilerOptions, baseUrl = _a.baseUrl, paths = _a.paths;
    return Object.keys(paths).reduce(function (aliases, pathName) {
        var alias = replaceGlobs(pathName);
        var path = replaceGlobs(paths[pathName][0]);
        aliases[alias] = path_1.resolve(dirname, baseUrl, path);
        return aliases;
    }, {});
};
